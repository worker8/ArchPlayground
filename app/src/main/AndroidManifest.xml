<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="github.com.worker8.archplayground">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TopActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".simpleMVP.SimpleMvvmActivity"
            android:theme="@style/BlueishAppTheme">

        </activity>

        <activity
            android:name=".simpleMVPRx.RxSimpleMvpActivity"
            android:theme="@style/BlueishAppTheme">

        </activity>

        <activity
            android:name=".rxFlatmapCombo.RxFlatmapComboActivity"
            android:theme="@style/BlueishAppTheme">

        </activity>

        <activity android:name=".rxRedux.RxReduxActivity">

        </activity>

    </application>
</manifest>
